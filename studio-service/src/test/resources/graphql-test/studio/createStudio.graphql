mutation CreateStudio(
    $organizationId: ID!
    $name: String!
    $description: String!
) {
    studios {
        createStudio(
            request: {
                organizationId: $organizationId
                name: $name
                description: $description
            }
        )
    }
}
