postgresql:
  enabled: true
  image:
    registry: docker.io
    repository: bitnamilegacy/postgresql
    tag: 15.4.0-debian-11-r45
  auth:
    existingSecret: "brand-service-postgresql"
  primary:
    persistence:
      enabled: true
      size: 1Gi
      accessModes:
        - ReadWriteOnce
      storage: 1Gi
      mountPath: /bitnami/postgresql
      storageClass: standard
    resources:
      requests:
        cpu: 125m
        memory: 256Mi
    livenessProbe:
      enabled: true
      initialDelaySeconds: 60
      periodSeconds: 10
      timeoutSeconds: 560
      failureThreshold: 6
      successThreshold: 1
    readinessProbe:
      enabled: true
      initialDelaySeconds: 60
      periodSeconds: 10
      timeoutSeconds: 5
      failureThreshold: 6
      successThreshold: 1
    startupProbe:
      enabled: true
      initialDelaySeconds: 30
      periodSeconds: 10
      timeoutSeconds: 5
      failureThreshold: 10
      successThreshold: 1
    initdb:
      scriptsSecret: brand-postgres-init-scripts

service:
  secrets:
    - name: brand-service-postgresql

liquibase:
  enabled: true
  secrets:
    - name: brand-service-liquibase

global:
  secrets:
    - name: brand-service-postgresql
      type: Opaque
      data:
        DBHOST: "brand-service-postgresql" # <release-name>-postgresql
        DBPORT: "5432"
        DBNAME: "brand_db"
        database: "brand_db"
        DBUSER: "brsportal"
        username: "brsportal"
        DBPASS: "brsportal"
        password: "brsportal"
        postgres-password: "pass"
    - name: brand-service-liquibase
      type: Opaque
      data:
        PGHOST: "brand-service-postgresql" # <release-name>-postgresql
        PGPORT: "5432"
        PGUSER: "brsliquibase"
        PGPASSWORD: "brsliquibase"
        PGDATABASE: "brand_db"
