query GetAppointment(
    $id: ID!
) {
    appointments {
        appointment(id: $id) {
            id
            description
            clientUserId
            masterUserId
            managerUserId
            studioId
            status
            comment
        }
    }
}
