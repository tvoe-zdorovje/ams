mutation CreateAppointment(
    $name: String!
    $description: String!
) {
    appointments {
        createAppointment(
            request: {
                name: $name
                description: $description
            }
        )
    }
}
