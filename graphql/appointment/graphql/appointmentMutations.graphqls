extend type AppointmentMutations {
    createAppointment(request: CreateAppointmentRequest!): ID!
}

input CreateAppointmentRequest {
    description: String!
    clientUserId: ID!
    masterUserId: ID!
    managerUserId: ID!
    studioId: ID!
    comment: String!
}
