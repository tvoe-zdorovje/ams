mutation UpdateUser(
    $id: ID!
    $firstName: String!
    $lastName: String!
    $phoneNumber: String!
) {
    users {
        updateUser(
            request: {
                id: $id
                firstName: $firstName
                lastName: $lastName
                phoneNumber: $phoneNumber
            }
        )
    }
}
