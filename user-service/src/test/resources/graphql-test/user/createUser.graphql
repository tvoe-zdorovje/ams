mutation CreateUser(
    $externalId: String!
    $firstName: String!
    $lastName: String!
    $phoneNumber: String!
) {
    users {
        createUser(
            request: {
                externalId: $externalId
                firstName: $firstName
                lastName: $lastName
                phoneNumber: $phoneNumber
            }
        )
    }
}
