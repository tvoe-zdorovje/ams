spring:
  profiles:
    active: local, h2
  cloud:
    config:
      enabled: false
  output:
    ansi:
      enabled: always
  kafka:
    listener:
      auto-startup: false
    admin.auto-create: false
