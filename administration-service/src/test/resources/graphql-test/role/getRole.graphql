query GetRole(
    $organizationId: ID!
    $roleId: ID!
) {
    roles {
        role(
            request: {
                organizationId: $organizationId
                roleId: $roleId
            }
        ) {
            id
            name
            description
            permissions {
                id
                name
                description
            }
        }
    }
}
