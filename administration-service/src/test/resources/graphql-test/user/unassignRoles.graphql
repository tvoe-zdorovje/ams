mutation UnassignRoles(
    $userId: ID!
    $organizationId: ID!
    $roles: [ID!]!
) {
    users {
        unassignRoles(
            request: {
                userId: $userId
                organizationId: $organizationId
                roles: $roles
            }
        )
    }
}
