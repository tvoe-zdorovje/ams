mutation AssignRoles(
    $userId: ID!
    $organizationId: ID!
    $roles: [ID!]!
) {
    users {
        assignRoles(
            request: {
                userId: $userId
                organizationId: $organizationId
                roles: $roles
            }
        )
    }
}
