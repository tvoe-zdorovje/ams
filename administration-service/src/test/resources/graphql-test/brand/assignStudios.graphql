mutation AssignStudios(
    $organizationId: ID!
    $studios: [ID!]!
) {
    brands {
        assignStudios(
            request: {
                organizationId: $organizationId
                studios: $studios
            }
        )
    }
}
