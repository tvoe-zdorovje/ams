mutation CreateRole(
    $organizationId: ID!
    $name: String!
    $description: String!
    $permissions: [ID!]
) {
    studios {
        createRole(
            request: {
                organizationId: $organizationId
                name: $name
                description: $description
                permissions: $permissions
            }
        )
    }
}
