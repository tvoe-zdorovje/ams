mutation CreateRole(
    $organizationId: ID!
    $name: String!
    $description: String!
    $permissions: [ID!]
) {
    brands {
        createRole(
            request: {
                organizationId: $organizationId
                name: $name
                description: $description
                permissions: $permissions
            }
        )
    }
}
